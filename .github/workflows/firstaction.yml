name: my first workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: echo "Hello RishiKushi"
      - name: print bye
        run: echo "Bye"
      - name: Print JAVA_HOME_17_X64
        run: echo $JAVA_HOME_17_X64
      - name: Check Maven Version
        run: mvn -version
